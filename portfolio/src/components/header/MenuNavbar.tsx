import Menu from './Menu';

const MenuNavbar = () => {
	return (
		<ul className="lg:hidden flex flex-wrap gap-[32px] font-semibold text-3xl text-purple0 ">
			<Menu />
		</ul>
	);
};

export default MenuNavbar;
